services:
  e2e:
    image: python:3.12.0a4-alpine3.17
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/automation   
    command: /bin/sh -c "ls -ls && python3 -m pip install . && pytest --version && pytest -m health --alluredir=allure-results"
#    command: /bin/sh -c "ls -ls"
    working_dir: /automation

#    environment:
#      - DB_USER=${DB_USER}
#      - DB_PASS=${DB_PASS}
#      - DB_NAME=${DB_NAME}
#      - DB_PORT=${DB_PORT}
#      - DB_HOST=${DB_HOST}

